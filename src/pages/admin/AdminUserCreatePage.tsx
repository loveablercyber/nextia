import{useState}from'react';import{UserPlus}from'lucide-react';
export default function AdminUserCreatePage(){const[form,setForm]=useState({name:'',email:'',password:'',role:'client',company:'',phone:'',cpfCnpj:'',partnerStatus:'pendente'});const[msg,setMsg]=useState('');const[error,setError]=useState('');const save=async(e:React.FormEvent)=>{e.preventDefault();setMsg('');setError('');const r=await fetch('/api/admin/users/create',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok){setError(d.error||'Falha ao cadastrar.');return}setMsg('Usuário cadastrado com sucesso.');setForm({name:'',email:'',password:'',role:'client',company:'',phone:'',cpfCnpj:'',partnerStatus:'pendente'})};return <div className="mx-auto max-w-3xl space-y-6"><header><h2 className="flex items-center gap-2 text-2xl font-black"><UserPlus/>Cadastro manual de usuário</h2><p className="text-slate-600">Crie clientes, parceiros, técnicos ou administradores.</p></header>{msg&&<p className="bg-green-50 p-4 text-green-700">{msg}</p>}{error&&<p className="bg-red-50 p-4 text-red-700">{error}</p>}<form onSubmit={save} className="grid gap-4 border bg-white p-6 sm:grid-cols-2"><Field label="Nome" value={form.name} onChange={v=>setForm({...form,name:v})}/><Field label="E-mail" type="email" value={form.email} onChange={v=>setForm({...form,email:v})}/><Field label="Senha inicial" type="password" value={form.password} onChange={v=>setForm({...form,password:v})}/><label className="font-bold">Perfil<select value={form.role} onChange={e=>setForm({...form,role:e.target.value})} className="mt-2 min-h-12 w-full border px-3"><option value="client">Cliente</option><option value="partner">Parceiro</option><option value="technician">Técnico</option><option value="admin">Administrador</option></select></label><Field label="Empresa" required={false} value={form.company} onChange={v=>setForm({...form,company:v})}/><Field label="Telefone" required={false} value={form.phone} onChange={v=>setForm({...form,phone:v})}/>{form.role==='partner'&&<><Field label="CPF/CNPJ" required={false} value={form.cpfCnpj} onChange={v=>setForm({...form,cpfCnpj:v})}/><label className="font-bold">Situação<select value={form.partnerStatus} onChange={e=>setForm({...form,partnerStatus:e.target.value})} className="mt-2 min-h-12 w-full border px-3"><option value="pendente">Pendente</option><option value="ativo">Ativo</option></select></label></>}<button className="min-h-12 bg-[#1677FF] font-bold text-white sm:col-span-2">Cadastrar usuário</button></form></div>}
function Field({label,value,onChange,type='text',required=true}:{label:string;value:string;onChange:(v:string)=>void;type?:string;required?:boolean}){return <label className="font-bold">{label}<input required={required} type={type} value={value} onChange={e=>onChange(e.target.value)} className="mt-2 min-h-12 w-full border px-3"/></label>}
